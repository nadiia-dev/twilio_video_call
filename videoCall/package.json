{
  "name": "videoCall",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.1",
    "axios": "^1.10.0",
    "twilio": "5.0.3",
    "twilio-video": "^2.31.0"
  },
  "devDependencies": {
    "twilio-run": "^4.2.0"
  },
  "engines": {
    "node": "22"
  }
}
